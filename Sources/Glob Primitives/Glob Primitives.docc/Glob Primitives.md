# ``Glob_Primitives``

@Metadata {
    @DisplayName("Glob Primitives")
    @TitleHeading("Swift Primitives")
}

L1 vocabulary for cross-platform glob pattern matching, defining the canonical pattern grammar consumed by POSIX and Windows L3-policy match implementations.

## Topics
